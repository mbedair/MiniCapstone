<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MiniCapstone" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MiniCapstone" parent="Base.Theme.MiniCapstone">
        <item name="colorPrimary">@color/orangeCustom3</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowSplashScreenBackground">@color/orangeCustom3</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_image_foreground</item>
    </style>

    <style name="splashScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_splash_logo</item>
        <item name="android:statusBarColor">@color/orangeCustom3</item>
    </style>
</resources>